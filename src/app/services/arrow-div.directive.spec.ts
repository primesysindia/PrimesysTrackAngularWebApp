import { ArrowDivDirective } from './arrow-div.directive';

describe('ArrowDivDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrowDivDirective();
    expect(directive).toBeTruthy();
  });
});
