import { SearchFileNamePipe } from './search-file-name.pipe';

describe('SearchFileNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFileNamePipe();
    expect(pipe).toBeTruthy();
  });
});
