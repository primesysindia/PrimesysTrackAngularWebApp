import { SerachNameRemarkPipe } from './serach-name-remark.pipe';

describe('SerachNameRemarkPipe', () => {
  it('create an instance', () => {
    const pipe = new SerachNameRemarkPipe();
    expect(pipe).toBeTruthy();
  });
});
