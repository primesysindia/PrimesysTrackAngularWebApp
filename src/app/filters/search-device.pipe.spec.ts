import { SearchDevicePipe } from './search-device.pipe';

describe('SearchDevicePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDevicePipe();
    expect(pipe).toBeTruthy();
  });
});
