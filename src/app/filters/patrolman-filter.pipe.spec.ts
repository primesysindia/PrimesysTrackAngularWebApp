import { PatrolmanFilterPipe } from './patrolman-filter.pipe';

describe('PatrolmanFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PatrolmanFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
