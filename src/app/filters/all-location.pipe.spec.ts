import { AllLocationPipe } from './all-location.pipe';

describe('AllLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new AllLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
