import { SortDevicesPipe } from './sort-devices.pipe';

describe('SortDevicesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortDevicesPipe();
    expect(pipe).toBeTruthy();
  });
});
