import { FindDevicePipe } from './find-device.pipe';

describe('FindDevicePipe', () => {
  it('create an instance', () => {
    const pipe = new FindDevicePipe();
    expect(pipe).toBeTruthy();
  });
});
